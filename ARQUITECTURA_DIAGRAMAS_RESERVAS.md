# 🏗️ ARQUITECTURA Y DIAGRAMA - CRUD Reservaciones

## 📐 Diagrama de Capas

```
┌─────────────────────────────────────────────────────────────┐
│                    CLIENTE (Angular/Mobile)                 │
│  (Components, Services, HTTP Requests con JWT)              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼ HTTP (JSON)
┌─────────────────────────────────────────────────────────────┐
│              CAPA DE PRESENTACIÓN (REST)                    │
│  ReservaController                                          │
│  ├─ @GetMapping("/list")                                  │
│  ├─ @GetMapping("/{id}")                                  │
│  ├─ @GetMapping("/estudiante/{id}")                       │
│  ├─ @GetMapping("/estudiante/{id}/activas")               │
│  ├─ @GetMapping("/disponibilidad/{id}")                   │
│  ├─ @PostMapping("/")                                     │
│  ├─ @PutMapping("/{id}")                                  │
│  ├─ @PutMapping("/{id}/cancelar")                         │
│  ├─ @PutMapping("/{id}/realizada")                        │
│  ├─ @PutMapping("/{id}/no-asistida")                      │
│  └─ @DeleteMapping("/{id}")                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│           CAPA DE NEGOCIO (Service)                         │
│  IReservaService (Interfaz)                                 │
│       └─ ReservaService (Implementación)                    │
│          ├─ Validaciones                                    │
│          ├─ Control de Aforo                                │
│          ├─ Prevención de Duplicados                        │
│          ├─ Transacciones                                   │
│          └─ Conversión a DTOs                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│         CAPA DE ACCESO A DATOS (Repository)                │
│  ReservaRepository extends JpaRepository                    │
│  ├─ findByIdEstudiante()                                  │
│  ├─ findByIdDisponibilidad()                              │
│  ├─ findByIdEstado()                                      │
│  ├─ findByIdEstudianteAndIdEstado()                       │
│  ├─ findReservaActivaDeEstudianteEnDisponibilidad() @Query│
│  ├─ countReservasActivasPorDisponibilidad() @Query        │
│  ├─ findReservasRealizadasDeEstudiante() @Query           │
│  └─ findReservasNoAsistidasDeEstudiante() @Query          │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼ SQL
┌─────────────────────────────────────────────────────────────┐
│              CAPA DE PERSISTENCIA (JPA)                     │
│  Reserva (Entidad JPA)                                      │
│  ├─ idReserva @Id @GeneratedValue                          │
│  ├─ idDisponibilidad @Column                               │
│  ├─ disponibilidad @ManyToOne                              │
│  ├─ idEstudiante @Column                                   │
│  ├─ estudiante @ManyToOne                                  │
│  ├─ idEstado @Column                                       │
│  ├─ estadoReserva @ManyToOne                               │
│  ├─ observaciones @Column                                  │
│  ├─ fechaCreacion @Column                                  │
│  ├─ fechaCancelacion @Column                               │
│  └─ razonCancelacion @Column                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼ SQL
┌─────────────────────────────────────────────────────────────┐
│              BASE DE DATOS (MySQL)                          │
│  reserva (Tabla)                                            │
│  ├─ id_reserva (PK)                                         │
│  ├─ id_disponibilidad (FK)                                 │
│  ├─ id_estudiante (FK)                                     │
│  ├─ id_estado (FK)                                         │
│  ├─ observaciones                                          │
│  ├─ fecha_creacion                                         │
│  ├─ fecha_cancelacion                                      │
│  └─ razon_cancelacion                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Datos - Crear Reserva

```
┌──────────────────────────────────────────────────────────────┐
│ CLIENTE (Angular/Postman)                                    │
│ POST /api/reservas/                                          │
│ Body: {idDisponibilidad: 1, idEstudiante: 9, ...}           │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ CONTROLLER (ReservaController)                               │
│ @PostMapping("/") crearReserva(CreateReservaDto)            │
│ ├─ Validación con @Valid                                    │
│ └─ Llama a reservaService.crearReserva()                    │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ SERVICE (ReservaService)                                     │
│ Valida:                                                      │
│ ├─ DTO no nulo                                              │
│ ├─ IDs positivos                                            │
│ ├─ Disponibilidad existe                                    │
│ ├─ Hay cupos disponibles (aforoDisponible > 0)              │
│ ├─ Estudiante existe                                        │
│ └─ No hay reserva activa previa                             │
│                                                              │
│ Operaciones:                                                │
│ ├─ Crear Reserva con estado=1                              │
│ ├─ Guardar en BD via repository                            │
│ ├─ Decrementar aforo                                        │
│ ├─ Guardar disponibilidad actualizada                      │
│ ├─ Convertir a ReservaResponseDto                          │
│ └─ Retornar DTO                                             │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ REPOSITORY (ReservaRepository)                               │
│ ├─ save(reserva) - Crea en BD                              │
│ ├─ findById(disponibilidad) - Verifica existe              │
│ └─ findByIdEstudianteAndIdEstado() - Previene duplicado    │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ BASE DE DATOS (MySQL)                                        │
│ INSERT INTO reserva (id_disponibilidad, id_estudiante, ...) │
│ UPDATE disponibilidad SET aforo_disponible = aforo_disponible - 1
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ RESPUESTA (201 Created)                                      │
│ {                                                            │
│   "idReserva": 1,                                            │
│   "idDisponibilidad": 1,                                     │
│   "idEstudiante": 9,                                         │
│   "idEstado": 1,                                             │
│   "nombreEstado": "Reservada",                               │
│   "observaciones": "...",                                    │
│   "fechaCreacion": "2025-11-07T10:30:00"                    │
│ }                                                            │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Datos - Cancelar Reserva

```
┌──────────────────────────────────────────────────────────────┐
│ CLIENTE                                                      │
│ PUT /api/reservas/1/cancelar                                 │
│ Body: {razonCancelacion: "No puedo asistir"}                │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ CONTROLLER                                                   │
│ @PutMapping("/{id}/cancelar")                               │
│ ├─ Extrae ID de ruta                                        │
│ ├─ Extrae razón del body                                    │
│ └─ Llama a reservaService.cancelarReserva()                 │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ SERVICE                                                      │
│ Valida:                                                      │
│ ├─ ID es positivo                                           │
│ ├─ Reserva existe                                           │
│ └─ No está ya cancelada (estado ≠ 2)                        │
│                                                              │
│ Operaciones:                                                │
│ ├─ Cambiar estado a 2 (Cancelada)                          │
│ ├─ Guardar razón cancelación                               │
│ ├─ Obtener disponibilidad                                   │
│ ├─ Incrementar aforoDisponible en 1                        │
│ ├─ Guardar disponibilidad                                   │
│ ├─ Convertir a ReservaResponseDto                          │
│ └─ Retornar DTO                                             │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ REPOSITORY                                                   │
│ ├─ findById(1) - Obtiene reserva                            │
│ ├─ save(reservaActualizada) - Actualiza                     │
│ └─ save(disponibilidad) - Libera cupo                       │
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ BASE DE DATOS                                                │
│ UPDATE reserva SET id_estado = 2, razon_cancelacion = ...   │
│ UPDATE disponibilidad SET aforo_disponible = aforo_disponible + 1
└──────────────┬───────────────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│ RESPUESTA (200 OK)                                           │
│ {                                                            │
│   "idReserva": 1,                                            │
│   "idEstado": 2,                                             │
│   "nombreEstado": "Cancelada",                               │
│   "razonCancelacion": "No puedo asistir",                   │
│   "fechaCancelacion": "2025-11-07T10:45:00"                 │
│ }                                                            │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗂️ Relaciones JPA

```
    ┌──────────────────┐
    │    Reserva       │
    ├──────────────────┤
    │ - idReserva (PK) │
    │ - idDisponibilidad
    │ - disponibilidad │──────┐
    │ - idEstudiante   │      │
    │ - estudiante     │──┐   │
    │ - idEstado       │  │   │
    │ - estadoReserva  │  │   │
    └──────────────────┘  │   │
                          │   │
                  ┌───────┘   │
                  │           │
                  ▼           ▼
            ┌─────────────┐  ┌──────────────────┐
            │ Estudiante  │  │ Disponibilidad   │
            ├─────────────┤  ├──────────────────┤
            │ - idUsuario │  │ - idDisponibilidad
            │ - usuario   │  │ - idTutoria      │
            │ - codigo    │  │ - fecha          │
            └─────────────┘  │ - horaInicio     │
                             │ - horaFin        │
                             │ - aforo          │
                             │ - aforoDisponible│
                             │ - estado         │
                             └──────────────────┘
                                     │
                                     ▼
                             ┌──────────────────┐
                             │    Tutoria       │
                             ├──────────────────┤
                             │ - idTutoria (FK) │
                             │ - nombre         │
                             │ - idTutor        │
                             │ - idCarrera      │
                             └──────────────────┘

    ┌──────────────────────┐
    │ EstadoReserva        │
    ├──────────────────────┤
    │ - idEstado (PK)      │
    │ - nombre             │
    │ - descripcion        │
    └──────────────────────┘
    
    Estados:
    1 = "Reservada"
    2 = "Cancelada"
    3 = "Realizada"
    4 = "No Asistida"
```

---

## 🔐 Ciclo de Seguridad

```
┌─────────────────────────────────────────────────────┐
│ 1. CLIENTE AUTENTICA                                │
│    POST /api/auth/login                             │
│    Body: {codigo, contrasena}                       │
│    Respuesta: {token: "JWT TOKEN"}                  │
└─────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│ 2. CLIENTE INCLUYE TOKEN EN HEADER                  │
│    Authorization: Bearer JWT_TOKEN                  │
└─────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│ 3. INTERCEPTOR VALIDA TOKEN                         │
│    AuthInterceptor                                  │
│    ├─ Extrae token del header                      │
│    ├─ Verifica firma                               │
│    ├─ Verifica expiración                          │
│    └─ Extrae claims (usuario, rol)                 │
└─────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│ 4. CONTROLLER VALIDA AUTORIZACIÓN                   │
│    @SecurityRequirement(name = "bearer-jwt")        │
│    ├─ Usuario debe ser Estudiante, Tutor o Admin   │
│    ├─ Validar acceso a datos del usuario           │
│    └─ Registrar auditoría de operación             │
└─────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│ 5. SERVICIO EJECUTA CON CONTEXTO SEGURO             │
│    Datos del usuario autenticado disponibles        │
│    Operación registrada en auditoría                │
└─────────────────────────────────────────────────────┘
```

---

## 📊 Estados y Transiciones

```
START
  │
  ▼
┌─────────────────┐
│  RESERVADA (1)  │◄────────────────────────────┐
│                 │                             │
│ - Activa        │                      Puede volver
│ - Puede cambiar │                      (si permitido)
└────┬────────────┘
     │ (Tiempo pasa)
     ├──────────────────────────────────────────────┐
     │                                              │
     ▼                                              ▼
┌──────────────┐                           ┌──────────────────┐
│ REALIZADA (3)│                           │  CANCELADA (2)   │
│              │                           │                  │
│ - Completada │                           │ - Cancelada      │
│ - Final      │                           │ - Permanente     │
└──────────────┘                           └──────────────────┘
                                                  ▲
                                                  │
                                            ┌─────────────────┐
                                            │ NO ASISTIDA (4) │
                                            │                 │
                                            │ - No asistió    │
                                            │ - Final         │
                                            └─────────────────┘
```

---

## 🧮 Lógica de Control de Aforo

```
CREAR RESERVA:
┌─────────────────────────────┐
│ Disponibilidad inicial:     │
│ aforo = 30                  │
│ aforoDisponible = 30        │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Verificar aforo > 0?        │
│ ✓ SÍ (30 > 0)              │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Crear Reserva               │
│ aforoDisponible--           │
│ Nuevo = 30 - 1 = 29         │
└─────────────────────────────┘

CANCELAR RESERVA:
┌─────────────────────────────┐
│ Disponibilidad con:         │
│ aforo = 30                  │
│ aforoDisponible = 5         │
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Cancelar Reserva            │
│ aforoDisponible++           │
│ Nuevo = 5 + 1 = 6           │
└─────────────────────────────┘
```

---

## 📋 Matriz de Validaciones

```
┌──────────────────────────────┬──────────┬─────────────────┐
│ Validación                   │ Punto    │ Resultado       │
├──────────────────────────────┼──────────┼─────────────────┤
│ DTO no nulo                  │ Service  │ 400 Bad Request │
│ IDs > 0                      │ Service  │ 400 Bad Request │
│ Disponibilidad existe        │ Service  │ 400 Bad Request │
│ Hay cupos (aforo > 0)        │ Service  │ 400 Bad Request │
│ Estudiante existe            │ Service  │ 400 Bad Request │
│ No hay duplicado activo      │ Service  │ 400 Bad Request │
│ Reserva existe (GET)         │ Service  │ 404 Not Found   │
│ Reserva no está cancelada    │ Service  │ 400 Bad Request │
│ Token JWT válido             │ Filter   │ 401 Unauthorized│
│ Rol autorizado               │ Filter   │ 403 Forbidden   │
└──────────────────────────────┴──────────┴─────────────────┘
```

---

## 🎯 Matriz de Responsabilidades

```
┌───────────────┬─────────────────┬──────────────┬─────────────────┐
│ Componente    │ Validación      │ Negocio      │ Persistencia    │
├───────────────┼─────────────────┼──────────────┼─────────────────┤
│ Controller    │ @Valid DTOs     │ Manejo de    │ Error HTTP      │
│               │ IDs válidos     │ errores      │                 │
│               │                 │              │                 │
│ Service       │ Lógica nego     │ Control      │ Llamadas        │
│               │ Validaciones    │ aforo        │ repository      │
│               │ Negocios        │ Transiciones │ Transacciones   │
│               │                 │ Estados      │                 │
│ Repository    │                 │              │ SQL Queries     │
│               │                 │              │ Persistencia    │
│               │                 │              │                 │
│ JPA/Hibernate │                 │              │ Mapeo ORM       │
│               │                 │              │ Relaciones      │
│               │                 │              │                 │
│ MySQL         │                 │              │ Almacenamiento  │
│               │                 │              │ Índices         │
└───────────────┴─────────────────┴──────────────┴─────────────────┘
```

---

**Diagrama Completado**: 7 de noviembre de 2025
